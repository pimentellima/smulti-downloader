{
    "name": "@smulti-downloader/convert-service",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "esbuild src/handler.ts --bundle --outdir=dist --format=cjs --minify=false --target=node22 --sourcemap --platform=node",
        "check-types": "tsc --noEmit"
    },
    "dependencies": {
        "@smulti-downloader/utils": "*",
        "@smulti-downloader/shared": "*",
        "@smulti-downloader/validations": "*",
        "drizzle-orm": "^0.30.0",
        "postgres": "^3.4.5",
        "aws-lambda": "^1.0.7"
    },
    "devDependencies": {
        "esbuild": "^0.21.1",
        "serverless-better-credentials": "^2.0.1",
        "serverless-esbuild": "^1.55.0",
        "@smulti-downloader/tsconfig": "*",
        "typescript": "^5.8.3",
        "@types/aws-lambda": "^8.10.114",
        "@types/node": "^20.11.30"
    }
}
